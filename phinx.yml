paths:
    migrations: %%PHINX_CONFIG_DIR%%/migrations

environments:
    default_migration_table: phinxlog
    default_database: development
    production:
        adapter: sqlite
        name: app/db/poche

    development:
        adapter: sqlite
        name: app/db/poche

    testing:
        adapter: sqlite
        name: app/db/poche
